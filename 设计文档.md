# 软著申请资料生成工具 — 软件设计文档

## 1. 概述

### 1.1 项目背景

将 [SoftwareCopyright-Skill](https://github.com/Fokkyp/SoftwareCopyright-Skill)（一个 Claude Code Skill）改造为独立的桌面应用。原始 Skill 依赖 Claude AI 模型进行业务判断和内容生成，本应用将其替换为 **DeepSeek API**，并用 **PyQt6** 构建桌面 GUI。

### 1.2 核心能力

自动生成中国软件著作权申请所需的全部材料：

| 输出文件 | 说明 |
|---------|------|
| `申请表信息.txt` | 软著登记申请表各字段 |
| `<软件名>-代码(前30页).docx` | 源代码鉴别材料（前30页） |
| `<软件名>-代码(后30页).docx` | 源代码鉴别材料（后30页） |
| `<软件名>_操作手册.docx` | 软件操作手册 |

---

## 2. 架构总览

```
┌────────────────────────────────────────────────────────┐
│                      桌面应用 (PyQt6)                    │
├────────────────────────────────────────────────────────┤
│  UI 层 (ui/)                                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐ │
│  │ main.py  │  │ 8 个步骤  │  │ main_window.py       │ │
│  │ 入口     │  │ 面板组件  │  │ 步骤导航 + 主布局     │ │
│  └──────────┘  └──────────┘  └──────────────────────┘ │
├────────────────────────────────────────────────────────┤
│  核心层 (core/)                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────┐ │
│  │ state_   │  │ deepseek_ │  │ prompts  │  │runner │ │
│  │ machine  │  │ client    │  │ .py      │  │.py    │ │
│  │ 状态机   │  │ API 封装  │  │ 5个Prompt│  │脚本调用│ │
│  └──────────┘  └──────────┘  └──────────┘  └───────┘ │
├────────────────────────────────────────────────────────┤
│  脚本层 (scripts/) — 10 个原始 Python 脚本               │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐ │
│  │ common   │  │ analyze  │  │ build_docx           │ │
│  │ .py      │  │ _project │  │ _from_md.py          │ │
│  │ 公共工具 │  │ 项目分析 │  │ Word/TXT 生成         │ │
│  └──────────┘  └──────────┘  └──────────────────────┘ │
├────────────────────────────────────────────────────────┤
│  外部服务                                              │
│  ┌──────────────────────────────────────┐             │
│  │     DeepSeek API (deepseek-chat)     │             │
│  │     128K 上下文 / 中文优化            │             │
│  └──────────────────────────────────────┘             │
└────────────────────────────────────────────────────────┘
```

### 2.1 分层职责

| 层 | 职责 | 具体做什么 |
|----|------|-----------|
| **UI 层** | 用户交互 | 显示步骤面板、收集输入、展示 AI 结果、触发操作 |
| **核心层** | 业务逻辑 | 管理状态、调用 AI、编排脚本、转换数据 |
| **脚本层** | 底层执行 | 项目扫描、代码抽取、DOCX 生成、环境检查 |
| **外部服务** | AI 能力 | 业务理解、代码选择、操作手册生成 |

### 2.2 数据流向

```
用户操作 → UI 面板 → 核心层（state_machine + prompts）→ DeepSeek API
                                              ↓
用户预览 ← UI 面板 ← 核心层（解析 AI 结果）← API 返回
    ↓
用户确认 → UI 面板 → runner.py → scripts/*.py → 输出文件
```

---

## 3. 工作流设计（8 步骤）

```
[1.环境检查] → [2.选择项目] → [3.业务理解] → [4.代码选择]
     ↓               ↓              ↓              ↓
    确认环境      分析项目      AI 判断行业     AI 选文件
                 预填名称      确认功能        确认选择
     ↓               ↓              ↓              ↓
[5.申请表] → [6.截图] → [7.操作手册] → [8.生成资料]
     ↓           ↓           ↓              ↓
  AI 填充      管理截图    AI 写手册      生成 Word
  用户补全     或跳过      自检修正       输出目录
```

### 3.1 每步详细逻辑

#### 步骤1：环境检查

```
触发: 用户点击「开始环境检查」
  ├─ 检查 Python 版本
  ├─ 检查 python-docx 是否安装
  ├─ 检查 openai SDK 是否安装
  ├─ 创建输出目录 软件著作权申请资料/
  ├─ 检查 DeepSeek API Key 是否配置
  └─ 显示结果

触发: 用户点击「确认环境」
  ├─ 运行 scripts/check_environment.py
  │   → 写入 环境检查.json（含 requires_user_input 标记）
  ├─ 如果 .NET SDK 缺失 → 弹出选择对话框
  │   ├─ 选择「兜底继续」→ 运行 confirm_stage.py 解除门禁
  │   └─ 选择「安装完整环境」→ 提示用户安装 .NET SDK
  └─ 标记 environment 阶段完成，跳转步骤2
```

**门禁**: `confirm_stage.py --stage environment`

#### 步骤2：选择项目

```
触发: 自动扫描当前目录
  ├─ 查找 package.json / pyproject.toml 等项目标识
  └─ 在列表显示候选项目

触发: 用户选择项目 → 点击「分析项目」
  ├─ 运行 scripts/analyze_project.py
  │   → 解析 package.json、路由、页面、组件
  │   → 输出 analysis/project.json
  ├─ 自动预填软件名称（从 package.json name 推断）
  ├─ 自动预填版本号（从 package.json version 推断）
  └─ 显示分析摘要

触发: 用户点击「确认项目」
  └─ 标记 project 阶段完成，跳转步骤3
```

**脚本输出**: `analysis/project.json` — 包含框架、语言、路由、文件分类等

#### 步骤3：AI 业务理解

```
触发: 用户点击「AI 生成业务理解」
  ├─ (1) 运行 scripts/generate_business_context.py（无 --model-context）
  │      → 收集项目证据：README、路由、页面文件、组件
  │      → 输出 草稿/业务理解证据.md + 业务理解证据.json
  │
  ├─ (2) 构建 BUSINESS_UNDERSTANDING Prompt
  │      → 从 project.json 提取：框架、语言、功能候选
  │      → 从 README 提取：项目描述
  │      → 从 categorized_files 提取：入口、页面、组件路径
  │
  ├─ (3) 调用 DeepSeek API
  │      → Model: deepseek-chat
  │      → Temperature: 0.3
  │      → Max tokens: 16000
  │      → Response format: JSON
  │
  ├─ (4) 解析 AI 返回的 JSON
  │      → 必需字段: product_positioning, industry, target_users,
  │                   core_value, business_features,
  │                   manual_modules (含 purpose/usage/entry/
  │                   operation_steps/feedback)
  │      → 显示在预览编辑器中（用户可手动修改）
  │
触发: 用户确认并点击「确认业务理解」
  ├─ 读取预览编辑器中的 JSON（支持用户修改）
  ├─ 写入临时文件 → 调用 scripts/generate_business_context.py --model-context
  │   → 输出 草稿/业务理解.md + 业务理解.json
  ├─ state.business_context = 业务理解.json 内容
  └─ 标记 business 阶段完成，跳转步骤4
```

**AI Prompt 关键约束**:
- manual_modules 按真实页面/路由填写，不能用 `auth/query/form` 模板
- 必须包含 usage（用户在什么场景下使用）和 operation_steps（具体操作动作）
- 所有判断必须能从项目证据中溯源

#### 步骤4：AI 代码选择

```
触发: 用户点击「生成候选清单」
  ├─ 运行 scripts/propose_code_selection.py
  │   → 扫描所有源码文件，按优先级排序（入口 > 路由 > 页面 > API > 组件 > 工具）
  │   → 输出 草稿/代码文件选择.json + 代码文件候选清单.md
  ├─ 渲染为复选框列表（文件路径 + 行数 + 证据类型）
  └─ 显示统计：已选/候选 文件数、行数、页数

触发: 用户手动勾选/取消勾选
  ├─ 实时更新统计：选中文件数、行数、预估页数
  └─ 自动填写 model_reason 为「用户手动选择」

触发: 用户点击「AI 智能选择」
  ├─ 构建 CODE_SELECTION Prompt
  │   → 传入：业务功能列表 + 候选文件名/行数/证据类型
  │   → 目标：≥60 页（约 3000 行）
  ├─ 调用 DeepSeek API
  ├─ 解析返回的 files 数组，自动勾选 AI 推荐的文件
  └─ 刷新列表（AI 选择的文件带 model_reason）

触发: 用户点击「确认选择」
  ├─ 至少选中 1 个文件（否则警告）
  ├─ 标记 user_confirmed: true → 写入 代码文件选择.json
  └─ 标记 code-selection 阶段完成，跳转步骤5
```

**优先级规则**（来自 `category_weight`）:

| 优先级 | 类型 | 示例 |
|--------|------|------|
| 0 | 入口文件 | `main.ts`, `App.vue` |
| 10 | 路由 | `router/index.ts` |
| 20 | 页面 | `pages/Dashboard.vue` |
| 30 | API/数据 | `api/user.ts` |
| 40 | 状态管理 | `store/index.ts` |
| 50 | 组件 | `components/Table.vue` |
| 60 | 工具 | `utils/format.ts` |
| 90 | 样式 | `styles/main.css` |

#### 步骤5：申请表信息

```
触发: 用户点击「生成代码材料」
  ├─ 运行 scripts/extract_code_material.py
  │   → 读取 代码文件选择.json 中 selected=true 的文件
  │   → 按 50 行/页分页
  │   → ≥60 页：生成 代码-前30页.md + 代码-后30页.md
  │   → <60 页：生成 代码-全部.md
  │   → 输出 草稿/代码提取清单.json
  ├─ 自动填充「源程序量」字段
  └─ 显示页数统计

触发: 用户点击「AI 智能填充」
  ├─ 构建 APPLICATION_FORM Prompt
  │   → 传入：软件名称、项目分析、业务理解
  ├─ 调用 DeepSeek API
  ├─ 自动填充：软件的主要功能（500-1300字）、开发目的、
  │             面向领域/行业、技术特点 等
  └─ 用户可在表单中手动修改任意字段

触发: 用户点击「确认申请表」
  ├─ 收集所有表单字段值 → 写入 user_answers.json
  ├─ 运行 scripts/generate_application_info.py
  │   → 输出 草稿/申请表信息.md
  ├─ state.app_fields = 用户填写值
  └─ 标记 application-fields 阶段完成，跳转步骤6
```

**字段约束**（遵循软著申请表规范）:
- 软件的主要功能：500~1300 字符
- 开发目的/硬件环境等：≤50 字符
- 技术特点：多选标签 + ≤100 字符文本
- 版本号：如项目版本 < V1.0，提醒用户确认是否用 V1.0

#### 步骤6：截图管理

```
触发: 用户选择截图方式
  ├─ 「跳过截图」：操作手册保留【截图预留：...】占位文本
  └─ 「自行截图」：打开截图目录，用户放入 PNG/JPG 文件

触发: 用户点击「添加截图文件」
  ├─ 打开文件选择对话框（支持多选）
  ├─ 复制到 截图/ 目录
  └─ 刷新截图列表

触发: 用户点击「确认截图设置」
  ├─ state.screenshot_method = "skip" | "user-supplied"
  └─ 标记 screenshot 阶段完成，跳转步骤7
```

#### 步骤7：AI 操作手册生成

```
触发: 用户点击「AI 生成操作手册」
  ├─ 构建 MANUAL_GENERATION Prompt
  │   → 传入：业务理解 JSON（含 manual_modules）、软件名称、版本号
  │   → System Prompt 包含完整格式规范：
  │     - 章节用中文大写序号（一、二、三...）
  │     - 相关文档用表格
  │     - 功能章节用段落（不用列表）
  │     - 禁止 AI 味套话
  │     - 禁止技术术语
  ├─ 调用 DeepSeek API（Temperature: 0.5, Max tokens: 16000）
  └─ 显示在预览编辑器中

触发: 用户点击「AI 自检修正」
  ├─ 构建 MANUAL_SELF_REVIEW Prompt
  │   → 传入：当前手册文本 + 模块数
  │   → 检查维度：章节完整性、AI味、技术术语、列表使用、内容厚度
  ├─ 调用 DeepSeek API
  ├─ 返回：issues_found[] + fixed_manual
  └─ 更新预览编辑器

触发: 用户手动修改手册内容
  └─ 直接在预览编辑器中编辑

触发: 用户点击「确认操作手册」
  ├─ 保存当前预览内容 → 草稿/操作手册.md
  └─ 标记 manual-preview 阶段完成，跳转步骤8
```

**自检规则**（来自原始脚本的 `manual_quality_issues()`）:
- 章节完整性：是否包含相关文档、说明、功能特点、系统要求、FAQ、术语表
- AI 味检测：是否包含"旨在、赋能、一站式"等套话
- 技术术语：是否包含"React、Vue、API、状态管理"等
- 模板化表达：是否用了"进入方式：/页面内容：/操作步骤："等制式标题
- 列表滥用：正文是否用了项目符号或编号列表

#### 步骤8：生成正式资料

```
触发: 用户点击「生成正式 Word/TXT」
  ├─ 校验：所有前置阶段已完成
  ├─ 运行 scripts/build_docx_from_md.py
  │   ├─ 读取 草稿/申请表信息.md → 最终软件名称和版本号
  │   ├─ 生成 正式资料/申请表信息.txt
  │   ├─ 生成 正式资料/<软件名>-代码(前30页).docx
  │   │   → 页眉：软件全称 版本号（左）第N页（右）
  │   │   → 字体：Consolas 7.2pt，紧凑行距
  │   ├─ 生成 正式资料/<软件名>-代码(后30页).docx
  │   │   → 页码从 31 开始
  │   ├─ 生成 正式资料/<软件名>_操作手册.docx
  │   │   → 页眉同代码文档
  │   │   → 正文：SimSun 10.5pt
  │   │   → 标题：中文大写序号样式
  │   └─ 输出 正式资料/生成报告.md
  ├─ 显示生成报告（含文件列表、警告、校验结果）
  └─ 用户点击「打开输出目录」查看文件
```

**DOCX 生成策略**:
1. 优先使用 python-docx 库（精确控制页眉、字体、页边距）
2. 兜底使用纯 OpenXML 生成（zipfile + XML 拼装）
3. 最后兜底使用 pandoc 转换
4. 所有文档强制黑色字体（`force_black_document`）

---

## 4. 状态机设计

### 4.1 状态定义

```python
class Stage(Enum):
    ENVIRONMENT       = "environment"        # 环境检查
    PROJECT           = "project"            # 项目选择
    BUSINESS          = "business"           # 业务理解
    CODE_SELECTION    = "code-selection"     # 代码选择
    APPLICATION_FIELDS = "application-fields" # 申请表
    SCREENSHOT        = "screenshot"         # 截图管理
    MANUAL_PREVIEW    = "manual-preview"     # 手册预览
    GENERATE          = "generate"           # 生成资料
```

### 4.2 状态转换规则

```
初始状态: current_stage = ENVIRONMENT

任何时刻:
  - 只能进入「所有前置阶段已完成」或「自身已完成」的阶段
  - 点击左侧导航：只能点击已完成或当前可进入的阶段
  - 点击下一步：跳转到下一个已完成前置的阶段
  - 阶段完成：自动标记 + 自动跳转下一步
```

### 4.3 WorkflowState 数据结构

```python
@dataclass
class WorkflowState:
    # 项目信息
    project_dir: Path | None        # 项目路径
    software_name: str              # 软件全称
    version: str                    # 版本号

    # 阶段进度
    current_stage: Stage            # 当前阶段
    completed_stages: set[Stage]    # 已完成阶段

    # 各阶段数据缓存
    analysis: dict | None           # 项目分析结果
    business_context: dict | None   # 业务理解 JSON
    code_selection: dict | None     # 代码选择 JSON
    code_manifest: dict | None      # 代码提取清单
    app_fields: dict                # 申请表字段
    screenshot_method: str          # 截图方式

    # AI 中间结果（可编辑）
    ai_business_json: dict | None
    ai_code_selection_updates: list | None
    ai_manual_text: str
```

---

## 5. AI 集成设计

### 5.1 DeepSeek API 封装

```python
class DeepSeekClient:
    def chat(user_prompt, system_prompt, temperature, max_tokens, response_json)
        → str                              # 返回文本

    def chat_json(user_prompt, system_prompt, temperature, max_tokens)
        → dict[str, Any]                   # 返回 JSON（自动解析）
```

**配置**:
- 端点: `https://api.deepseek.com`（可自定义）
- 模型: `deepseek-chat`
- API Key: 环境变量 `DEEPSEEK_API_KEY` 或配置文件 `~/.soft-copyright/config.json`
- 兼容 OpenAI SDK，使用 `from openai import OpenAI`

### 5.2 5 个 AI Prompt 模板

| 序号 | Prompt | 触发时机 | 输入 | 输出 |
|------|--------|---------|------|------|
| 1 | `BUSINESS_UNDERSTANDING` | 步骤3 | 项目分析 + 路由 + 文件结构 | 业务理解 JSON |
| 2 | `CODE_SELECTION` | 步骤4 | 候选文件 + 业务功能 | 文件选择 JSON |
| 3 | `APPLICATION_FORM` | 步骤5 | 项目分析 + 业务理解 | 字段建议 JSON |
| 4 | `MANUAL_GENERATION` | 步骤7 | 业务理解 + 模块列表 | 操作手册 Markdown |
| 5 | `MANUAL_SELF_REVIEW` | 步骤7 | 手册草稿 | 问题列表 + 修正文本 |

### 5.3 Prompt 设计原则

每个 Prompt 都遵循以下原则（从原始 SKILL.md 提炼）：

1. **System Prompt**: 包含角色定义 + 领域知识 + 输出格式规范 + 软著申请约束
2. **User Prompt**: 只传项目真实数据，不包含规则（规则全在 System Prompt）
3. **JSON 优先**: 结构化输出使用 `response_format: {type: "json_object"}`
4. **证据溯源**: 所有判断必须能从项目代码/文档中找到证据
5. **防编造**: 数据不足时标记"待用户确认"而非编造

---

## 6. UI 架构

### 6.1 布局结构

```
┌──────────────────────────────────────────────┐
│  菜单栏: 设置 | 帮助                          │
├────────┬─────────────────────────────────────┤
│ 左侧    │ 右侧内容区                          │
│ 导航栏  │                                    │
│ 200px   │ ┌────────────────────────────────┐ │
│         │ │ 步骤标题 + 说明                 │ │
│ 1. 环境 │ ├────────────────────────────────┤ │
│ 2. 项目 │ │                                │ │
│ 3. 业务 │ │ 步骤专属内容区                   │ │
│ 4. 代码 │ │ （表单/预览/列表/按钮）          │ │
│ 5. 申请 │ │                                │ │
│ 6. 截图 │ ├────────────────────────────────┤ │
│ 7. 手册 │ │ [← 上一步]        [下一步 →]   │ │
│ 8. 生成 │ └────────────────────────────────┘ │
└────────┴─────────────────────────────────────┘
```

### 6.2 导航栏状态

```
未激活:  1. 环境检查          灰色文字
当前步:  2. 选择项目          蓝色高亮
已完成:  ✓ 1. 环境检查        绿色 + ✓
已锁定:  3. 业务理解          灰色（不可点击）
```

### 6.3 步骤面板通用模式

每个步骤面板遵循统一模式：

```python
class XxxStep(QWidget):
    stage_completed = pyqtSignal(Stage)  # 完成信号

    def __init__(self):
        # 1. 标题 + 说明
        # 2. 数据展示区（QTextEdit/QListWidget/表单）
        # 3. 操作按钮区
        # 4. 确认按钮 → 触发 stage_completed

    def refresh(self):
        # 从 state 加载最新数据刷新面板
```

### 6.4 后台线程处理

AI 调用通过 `QThread` 异步执行，避免阻塞 UI：

```python
class AIXxxThread(QThread):
    finished_with_result = pyqtSignal(object, object)  # (result, error)

    def run(self):
        # 调用 DeepSeek API
        # 捕获异常并通过 signal 传回主线程
```

**状态反馈**:
- 按钮禁用 + 文字变为 "⏳ AI 正在分析..."
- QProgressBar 显示动画（不确定模式）
- API 返回后恢复按钮 + 更新内容

---

## 7. 脚本层说明

### 7.1 脚本清单

| 脚本 | 功能 | 输入 | 输出 |
|------|------|------|------|
| `check_environment.py` | 检查运行时能力 | -- | `环境检查.json/md` |
| `analyze_project.py` | 项目结构分析 | `--project` | `analysis/project.json` |
| `generate_business_context.py` | 业务证据收集+写入 | `--project --analysis --model-context` | `草稿/业务理解.json/md` |
| `propose_code_selection.py` | 代码候选清单 | `--project --out-dir` | `草稿/代码文件选择.json` |
| `extract_code_material.py` | 抽取代码材料 | `--project --selection --out-dir` | `草稿/代码-前30页.md` 等 |
| `generate_application_info.py` | 申请表草稿 | `--analysis --code-manifest --business-context` | `草稿/申请表信息.md` |
| `generate_manual_draft.py` | 操作手册草稿 | `--analysis --business-context --out-dir` | `草稿/操作手册.md` |
| `build_docx_from_md.py` | 生成 Word/TXT | `--workdir --software-name --version` | `正式资料/*.docx/.txt` |
| `confirm_stage.py` | 记录阶段门禁 | `--workdir --stage --note` | 门禁确认文件 |
| `capture_screenshots.py` | 整理用户截图 | `--manual-dir --out-dir` | `截图/截图清单.json` |

### 7.2 调用方式

脚本通过 `core/runner.py` 以子进程方式调用：

```python
def _run_script(script_name: str, args: list[str]):
    cmd = [sys.executable, str(script_path)] + args
    result = subprocess.run(cmd, capture_output=True, text=True)
    return result.returncode, result.stdout, result.stderr
```

**优势**:
- 脚本与 GUI 进程隔离，崩溃不影响界面
- 保持原始脚本的 CLI 兼容性
- 可以单独在命令行中调试脚本

---

## 8. 配置管理

### 8.1 配置文件位置

```
~/.soft-copyright/config.json
```

### 8.2 配置项

```json
{
  "deepseek_api_key": "sk-...",
  "deepseek_base_url": "https://api.deepseek.com",
  "deepseek_model": "deepseek-chat",
  "output_dir": "",
  "language": "zh-CN"
}
```

### 8.3 API Key 优先级

```
环境变量 DEEPSEEK_API_KEY  >  配置文件 config.json
```

---

## 9. 错误处理策略

| 场景 | 处理方式 |
|------|---------|
| DeepSeek API 超时 | 显示错误信息 + 允许重试 |
| DeepSeek API 返回非 JSON | 尝试正则提取 JSON 块，失败则提示重试 |
| 脚本执行失败 | 显示 stderr + 允许重试 |
| 用户未选择文件就确认 | 弹出警告对话框 |
| 前置阶段未完成 | 禁用确认按钮 + 高亮缺失步骤 |
| 输出目录无写权限 | 脚本报错，UI 显示错误 |
| python-docx 未安装 | 自动使用纯 OpenXML 兜底 |

---

## 10. 打包分发

使用 PyInstaller 打包为单个 `.exe` 文件：

```bash
pyinstaller --onefile --windowed \
  --name "ruanzhu" \
  --add-data "scripts:scripts" \
  --add-data "vendor:vendor" \
  --hidden-import PyQt6 \
  --hidden-import openai \
  --hidden-import docx \
  main.py
```

