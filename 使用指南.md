# 软著申请资料生成工具 — 使用指南

## 一、安装配置

### 1.1 环境要求

- Windows 10/11 或 macOS 13+
- Python 3.10+（已安装则跳过）

### 1.2 安装依赖

打开终端（cmd 或 PowerShell），进入软件目录：

```bash
cd "C:\Users\yaowe\Desktop\软件著作权申请工具"
pip install -r requirements.txt
```

### 1.3 配置 DeepSeek API Key

1. 打开 [platform.deepseek.com](https://platform.deepseek.com)
2. 注册账号 → API Keys → 创建 API Key → 复制
3. 启动软件：`python main.py`
4. 菜单栏 **设置 → API 配置**
5. 粘贴 API Key → 点击「测试连接」→ 看到 ✓ 即可

> API Key 只需配置一次，之后会自动保存。

---

## 二、快速上手（8步完成）

### 步骤1：环境检查

点击「🔍 开始环境检查」，确认：
- Python 版本正常
- python-docx 已安装
- openai SDK 已安装
- DeepSeek API Key 已配置

如果提示 DOCX 环境不完整，选择 **「兜底继续（推荐）」** 即可。

点击「✓ 确认环境」进入下一步。

---

### 步骤2：选择项目

软件会自动扫描当前目录中的项目。

1. 双击列表中的项目，或点击「浏览」手动选择
2. 点击「🔍 分析项目」
3. 检查分析结果（框架、语言、文件数）
4. 软件名称和版本号自动从 `package.json` 预填

点击「✓ 确认项目」。

---

### 步骤3：AI 业务理解

这是第一个 AI 节点，DeepSeek 会分析代码判断软件的业务属性。

1. 确认软件全称和版本号（可修改）
2. 点击「🤖 AI 生成业务理解」→ 等待 10-30 秒
3. 左侧会出现 JSON 预览，包含：
   - 产品定位、行业领域
   - 目标用户、核心价值
   - 主要功能列表
   - 操作手册模块结构
4. **可手动修改** JSON 中的任何内容
5. 确认无误后点击「✓ 确认业务理解」

---

### 步骤4：AI 代码选择

选择哪些源码文件提交到软著材料中。

1. 点击「📋 生成候选清单」→ 列出所有源码文件
2. 点击「🤖 AI 智能选择」→ DeepSeek 自动挑选最优文件
3. 也可以手动勾选/取消勾选
4. 注意看统计：**至少需要 60 页（约 3000 行）**
5. 点击「✓ 确认选择」

---

### 步骤5：申请表信息

填写软著登记的申请表各字段。

1. **先点击「📄 先生成代码材料」**（这步不能跳过！）
2. 点击「🤖 AI 智能填充」→ 自动填好大部分字段
3. 手动检查并补全：
   - **开发完成日期**（格式 YYYY-MM-DD）
   - **著作权人**信息
   - **软件的主要功能**（500-1300 字）
   - 硬件/系统环境
4. 点击「✓ 确认申请表」

---

### 步骤6：截图管理

为操作手册准备截图。

- 选择 **「跳过截图」**：操作手册保留 `【截图预留】` 位置，后续可手动插入
- 选择 **「自行截图」**：可以点击「添加截图」选择图片文件

> 截图不影响材料生成，跳过也完全可以提交。

---

### 步骤7：AI 操作手册生成

DeepSeek 自动撰写完整的软件操作手册。

1. 点击「🤖 AI 生成操作手册」→ 等待 30-60 秒
2. 右侧预览区显示完整手册
3. 点击「🔍 AI 自检修正」→ 检查 AI 味、技术术语、格式问题
4. **可手动编辑**预览区的内容
5. 确认后点击「✓ 确认操作手册」

---

### 步骤8：生成正式资料

最后一步，一键生成所有文件。

1. 点击「🎉 生成正式 Word/TXT 文件」
2. 等待几秒钟
3. 点击「📂 打开输出目录」

---

## 三、输出文件

生成后在项目目录下：

```
软件著作权申请资料/
└── <项目名>_20260605/
    ├── analysis/                          # 分析数据
    ├── 草稿/                              # 中间草稿
    └── 正式资料/                          # ★ 最终提交文件
        ├── 申请表信息.txt                  # 申请表各字段
        ├── <软件名>-代码(前30页).docx       # 源码前30页
        ├── <软件名>-代码(后30页).docx       # 源码后30页
        ├── <软件名>_操作手册.docx           # 操作手册
        └── 生成报告.md                     # 生成记录
```

**最终提交到版权保护中心的文件就是 `正式资料/` 下的这些。**

---

## 四、常见问题

### Q: API Key 在哪获取？
去 [platform.deepseek.com](https://platform.deepseek.com) 注册，在 API Keys 页面创建。新用户通常有免费额度。

### Q: AI 分析结果不对怎么办？
每个 AI 生成步骤都有预览和编辑区，你可以直接修改内容后再确认。

### Q: "待用户确认"是什么？
原始脚本的字段占位符。在桌面应用里这些会在生成前自动清理，不影响最终输出。

### Q: 代码材料不足 60 页怎么办？
回到步骤4，手动多勾选一些文件。或者 AI 会自动尝试补齐。

### Q: .NET SDK 需要装吗？
不需要。选择「兜底继续」即可，python-docx 完全够用。

### Q: 每次生成会覆盖之前的结果吗？
不会。每次分析新项目会自动创建带日期的独立子目录，如 `MyProject_20260605/`。

### Q: DeepSeek API 调用大概花多少钱？
按 DeepSeek 当前定价，生成一套完整的软著材料（含多次 AI 调用）大约 **0.1-0.3 元人民币**。

---

## 五、快捷键和技巧

| 操作 | 说明 |
|------|------|
| 左侧导航栏 | 点击已完成步骤可随时跳回查看 |
| 上一步/下一步 | 底部按钮，可在已完成步骤间自由切换 |
| 预览编辑 | 步骤3 和步骤7 的预览区支持直接编辑 |
| 手动选择代码 | 步骤4 可以自行勾选，不需要等 AI |
